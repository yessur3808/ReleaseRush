
APP_NAME="ReleaseRush"
GA_MEASUREMENT_ID="XXXXXXXXXX"

# Base URL of the running Game-Tracker-Service.
# When set, the app fetches live game data from the service instead of
# the bundled static games.json fallback.
# Example (local dev): GAMES_API_URL=http://localhost:3000
# Example (production): GAMES_API_URL=https://api.releaserush.com
GAMES_API_URL=
